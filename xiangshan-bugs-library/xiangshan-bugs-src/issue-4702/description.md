According to the manual description, cbo instr (inval clean flush) requires read permission but throws store exception.

Considering that NEMU has multiple commits for other content modifications, I'll mention a separate bump NEMU pr

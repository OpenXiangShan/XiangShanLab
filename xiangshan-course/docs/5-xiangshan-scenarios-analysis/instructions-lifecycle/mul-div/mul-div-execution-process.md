# 乘法与除法执行过程：阅读入口

乘除法指令的执行过程已按执行延迟和唤醒机制拆为两篇独立文档。原有执行波形分别保留在对应文章中，下面两篇均可独立阅读。

* [一条乘法指令的执行过程：固定延迟与提前唤醒](mul-execution-process.md)：从译码、重命名到回写和提交，进一步分析 `mul + add` 如何利用固定延迟安排 IQ 提前唤醒，并通过 Bypass 取得结果。
* [一条除法指令的执行过程：不固定延迟与回写唤醒](div-execution-process.md)：从 `divu` 的依赖建立讲到非流水化除法器的握手、迭代和完成，再分析 `divu + add` 的回写唤醒以及发射失败后的重试。

| 对比项 | 乘法 | 除法 |
| --- | --- | --- |
| 本文核对的配置 | `piped = true`、`CertainLatency(2)` | `piped = false`、`UncertainLatency()` |
| 结果时序 | 正常执行路径下可按配置安排 | 等待实际运算完成 |
| 相关示例的消费者唤醒 | IQ 侧延迟队列安排提前唤醒 | 实际回写经 `WakeUpFromWB` 唤醒 |
| 共同约束 | 源就绪与资源允许后发射，执行前选取正确数据，按 ROB 顺序提交 | 同左 |


